# book-management-rest-api
A simple REST API built using Node.js and Express.js to manage a list of books with CRUD operations. The project demonstrates Express routing, HTTP methods, JSON request handling, middleware, and API testing using Postman. Books are stored in memory without using a database, making it ideal for learning REST API development.
